name: Laravel

on:
  push:
    branches: [ "main" ]

jobs:
  job1:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: customize step1
      run: pwd

  job2:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: customize step1
      run: ls
    
    
